% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_bdl.R
\name{get_bdl}
\alias{get_bdl}
\title{Query BDL data}
\usage{
get_bdl(
  variables = NULL,
  subjects = NULL,
  level = "gmina",
  years = NULL,
  units = NULL,
  unit_level = NULL,
  dims = NULL,
  aggregate = FALSE,
  add_labels = TRUE,
  path = bdl_path()
)
}
\arguments{
\item{variables}{Integer vector of variable IDs, or \code{NULL} for all variables
in the selected subjects. At least one of \code{variables} or \code{subjects} must be
provided.}

\item{subjects}{Character vector of subject codes (e.g. \code{"K3"}, \code{"P1312"}),
or \code{NULL}. All variables under each subject are included.}

\item{level}{Geographic level. One of \code{"gmina"} (unitLevel 6), \code{"powiat"}
(5), \code{"subregion"} (4), \code{"voivod"} (2), \code{"macroregion"} (1).
Default: \code{"gmina"}. Polish aliases (e.g. \code{"podregion"}, \code{"wojewodztwo"},
\code{"makroregion"}) are also accepted. No national-level (level 0) aggregate
is available.}

\item{years}{Integer vector of years to include, or \code{NULL} for all years.}

\item{units}{Character vector of unit IDs (12-char zero-padded GUS codes;
IDs that lost their leading zeros are normalised automatically) or unit
names, or \code{NULL} for all units.}

\item{unit_level}{Deprecated and ignored: each fact table now contains a
single unit level, selected via \code{level}.}

\item{dims}{Named list of dimension filters, e.g. \code{list(n1 = "ogółem")} or
\code{list(n1 = c("kobiety", "mężczyźni"), n2 = c("ogółem", NA))}. Each name
corresponds to a dimension column (\code{n1}–\code{n5}). \code{NA} in a value vector
matches rows where that column is empty. Applied via the codebook before
scanning facts, so only matching variables are read.}

\item{aggregate}{Logical. If \code{TRUE}, shorthand for filtering to aggregate
(ogółem) values across all dimensions — equivalent to
\code{dims = list(n1="ogółem", n2=c("ogółem",NA), n3=c("ogółem",NA))}.
Returns the "headline" figure without any cross-tabulation. Default \code{FALSE}.}

\item{add_labels}{Logical. If \code{TRUE} (default), joins variable metadata
(subject name, dimensions, measure unit) from the codebook.}

\item{path}{Path to the data store root. Defaults to \code{bdl_path()}.}
}
\value{
A \code{data.frame} with columns:
\code{variable_id}, \code{unitId}, \code{unitName}, \code{unitLevel}, \code{year}, \code{value},
and (if \code{add_labels = TRUE}) \code{subject_name}, \code{variable_dimensions},
\code{measureUnitName}, \code{variable_full_name}.
}
\description{
Main function for retrieving data from the local BDL store
(\code{bdl_gus_v2.duckdb}), which holds native fact tables per geographic level.
}
\examples{
\dontrun{
options(bdlR.path = "/Volumes/Samsung T72/Data/API GUS")

# Population totals for all gminy, all years
get_bdl(variables = 6, level = "gmina")

# Aggregate (ogółem) totals for a subject, all gminy, 2020
get_bdl(subjects = "P1312", level = "gmina", years = 2020, aggregate = TRUE)

# Female-only breakdown for a subject
get_bdl(subjects = "P1313", level = "gmina", years = 2020, dims = list(n1 = "kobiety"))

# Specific gminy by GUS unit ID (leading zeros optional)
get_bdl(variables = 6, units = c("011212001011", "21200000000"))

# All variables for one gmina — fast on the v2 store
get_bdl(subjects = "K3", units = "Bochnia", years = 2010:2024)
}
}
