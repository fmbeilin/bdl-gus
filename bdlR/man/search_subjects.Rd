% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{search_subjects}
\alias{search_subjects}
\title{Search subjects by keyword}
\usage{
search_subjects(query, level = NULL)
}
\arguments{
\item{query}{Character scalar. Keyword to search in subject names (Polish).}

\item{level}{Optional integer. Restrict to subjects available at a specific
unit level (\code{6} = gmina, \code{5} = powiat, \code{4} = subregion, \code{2} = voivodeship).
\code{NULL} returns all levels.}
}
\value{
A \code{data.frame} with columns:
\itemize{
\item \code{subjectId} — pass to \code{\link[=get_bdl]{get_bdl()}} or \code{\link[=get_subject_dims]{get_subject_dims()}}
\item \code{subject_name} — human-readable concept label
\item \code{n_variables} — total number of variant variables
\item \code{n_aggregate} — number of aggregate (ogółem) variants
\item \code{n1_breakdowns} — distinct values of the primary dimension (n1)
\item \code{example_variable_id} — smallest variable_id for quick lookup
}
}
\description{
Returns one row per matching subject (concept), not per variable. Use this
as the first step to discover what data is available without being
overwhelmed by dimension variants. Follow up with \code{\link[=get_subject_dims]{get_subject_dims()}} to
see available breakdowns, then pass the \code{subjectId} to \code{\link[=get_bdl]{get_bdl()}}.
}
\examples{
\dontrun{
options(bdlR.path = "/Volumes/Samsung T72/Data/API GUS")

search_subjects("bezrobocie")
search_subjects("ludność", level = 6)
}
}
