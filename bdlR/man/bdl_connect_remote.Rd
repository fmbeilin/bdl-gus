% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{bdl_connect_remote}
\alias{bdl_connect_remote}
\title{Open a DuckDB connection to the remote (Hugging Face) BDL store}
\usage{
bdl_connect_remote(repo = bdl_remote_repo())
}
\arguments{
\item{repo}{Hugging Face dataset repo. Defaults to \code{\link[=bdl_remote_repo]{bdl_remote_repo()}}.}
}
\value{
A DBI connection object. Caller is responsible for closing with \code{DBI::dbDisconnect()}.
}
\description{
Creates an in-memory DuckDB with views over the parquet files hosted in the
Hugging Face dataset repo, so the same table names work as in the local
store (facts_gminy, ..., codebook, units, variables, subjects). Queries are
served over HTTP with predicate pushdown — only the row groups a query
needs are downloaded.
}
\details{
For private repos, a token is picked up from the \code{HF_TOKEN} environment
variable or the \code{hf} CLI login cache.
}
